# Copy to `.env` (which is gitignored) before running `docker compose up`.
# This password is consumed by docker-compose.yml for the SQL Server container
# and must match the password in NewsletterPreferences.Api/appsettings.json
# (ConnectionStrings:DefaultConnection).
SA_PASSWORD=NewsletterDev_2024!
